import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  verticalCentered: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  withPadding: {
    padding: 20
  },
});
