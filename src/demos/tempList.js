import React from 'react';

import ListFriendsChat from './ListChatFriends';
import ListContacts from './ListContacts';

const TempList = props => {
    return (
        <ListContacts />
    );
};

export default TempList;
